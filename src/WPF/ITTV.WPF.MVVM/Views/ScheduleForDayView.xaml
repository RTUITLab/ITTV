<UserControl x:Class="ITTV.WPF.MVVM.Views.ScheduleForDayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ITTV.WPF.MVVM.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid x:Name="mainGridmainGrid" VerticalAlignment="Center"
              Margin="100 50 100 50">
            <TextBlock
                Text="1">
            </TextBlock>
            <Rectangle
                Width="2">
            </Rectangle>
            <TextBlock
                Text="Some name of class">
                </TextBlock>
            <Rectangle
                Width="2">
            </Rectangle>
            
            <TextBlock x:Name="OutList"
                       Visibility="Collapsed"
                       Text="Пар в этот день нет"
                       HorizontalAlignment="Center"
                       FontWeight="Medium"
                       FontSize="40"/>
        </Grid>
    </ScrollViewer>
</UserControl>
