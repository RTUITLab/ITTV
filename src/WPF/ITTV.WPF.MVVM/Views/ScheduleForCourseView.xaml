<UserControl x:Class="ITTV.WPF.MVVM.Views.ScheduleForCourseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ITTV.WPF.MVVM.Views"
             xmlns:controls="http://schemas.microsoft.com/kinect/2014"
             xmlns:schedule="clr-namespace:ITTV.WPF.MVVM.ViewModels.Schedule"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance schedule:ScheduleForCourseViewModel,
                                                    IsDesignTimeCreatable=True}">
    <Grid>
        <ScrollViewer Margin="0 20 0 0"
                      controls:KinectRegion.IsScrollInertiaEnabled="true">
            <MediaElement x:Name="ScheduleForCourse"
                          Source="{Binding FilePath}"
                          UnloadedBehavior="Stop"/>
        </ScrollViewer>
    </Grid>
</UserControl>
